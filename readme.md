# Swarm+MBS TA model

This is the source code for a simulation model testing task assignment algorithm for a UAV swarm with mobile base station (MBS)


To use this:
install the Omnet++ 6.3 software
install latest version of INET package
create new project
put all files in src folder
build project
run simulation from omnetpp.ini file